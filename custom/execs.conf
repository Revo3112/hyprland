# Auto-start applications
# https://wiki.hyprland.org/Configuring/Keywords/#executing

# Display Management
exec-once = hyprdynamicmonitors run

# Notification daemon handled by Quickshell
# (org.freedesktop.Notifications is already owned by qs)

# Battery Monitor (systemd service: systemctl --user enable --now battery-monitor.service)
